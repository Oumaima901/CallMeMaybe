<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.contactapp">
    <!-- declaration des permissions -->
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/><!--file-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/call"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/call"
        android:supportsRtl="true"
        android:theme="@style/Theme.ContactApp">
        <activity android:name=".EditPage"></activity>
        <activity android:name=".ContactPage" />
        <activity android:name=".AddContact" />
        <activity android:name=".HomePage" />
        <activity android:name=".login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>